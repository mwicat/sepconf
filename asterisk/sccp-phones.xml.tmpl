#attr $path /etc/asterisk/sccp-phones.xml
#for $user in $users
[$user.sep]($user.phone)
description = $user.name
button = line, $user.line

[$user.line](defaultline)
label = $user.line
cid_name = $user.name
cid_num = $user.line

#end for
