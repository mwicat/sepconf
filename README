Download

git clone git://github.com/mwicat/sepconf.git

Dependencies

sudo apt-get install python-cheetah python-pip
sudo setup.py install

How to generate configuration for your phone:

edit custom.csv to match your phone

run:

sudo make generate install_freeswitch

or

sudo make generate install_asterisk


Generate 5 users

./sepgen.py generate 5 --format csv > users.csv

Pickle CSV users

./sepgen.py transform > dist/users.pickle < users.csv

Fill a template

cheetah fill --pickle dist/users.pickle --stdout templates/sccp_phones_asterisk.tmpl

Reload freeswitch - reloadxml
Reload asterisk - sccp reload

